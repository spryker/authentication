post:
    refreshToken:
        - NotBlank:
              message: 'validation.not_blank'
        - Length:
              min: 10
              minMessage: 'validation.min_length'

delete:
    refreshToken:
        - NotBlank:
              message: 'validation.not_blank'
